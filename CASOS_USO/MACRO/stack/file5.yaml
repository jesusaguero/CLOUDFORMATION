AWSTemplateFormatVersion: "2010-09-09"
Transform: ComputeValuesMacro  # Duplicará el valor de "Size"

Resources:
  MyVolume:
    Type: AWS::EC2::Volume
    Properties:
      Size: 50  # La macro lo cambiará a 100
